import { useState } from "react";
import Chart from "../Farhan Chart/Chart.js";
import { Bar } from "react-chartjs-2";

const Chart = ({ data }) => {
  const [chartData, setChartData] = useState(data);
};
